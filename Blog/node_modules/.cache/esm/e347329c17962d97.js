let express,postRoutes,authRoutes;_179‍.w("express",[["default",["express"],function(v){express=v}]]);_179‍.w("./config/dbconnect");_179‍.w("./routers/postRoutes",[["default",["postRoutes"],function(v){postRoutes=v}]]);_179‍.w("./routers/authRoutes",[["default",["authRoutes"],function(v){authRoutes=v}]]);


// Connecting to database


//importing routes



const app = express();
app.use(express.json());

app.use("/auth", authRoutes);
app.use("/posts", postRoutes);

//catch all routes not on the server
app.all("*", (req, res, next) => {
    res
    .status(404)
    .json({ message: `cannot find ${req.originalUrl} on the server`});
})

//
app.use((error, req, res, next) => {
    res.status(error.status || 500).json({ message: error.message});
})

app.listen(4000, () => console.log("Server up and running"));
